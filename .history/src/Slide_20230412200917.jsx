const tada = [0, 1, 2, 3, 4, 5, 6, 0, 0, 0];
export const slide = (props) => {
  console.log(props);

  //   if (element.classList.contains("red") == true) {
  //     //何か処理を書く
  //   }

  return;
};
